const graphql = require('graphql');
const {
  GraphQLObjectType,
  GraphQLSchema,
  GraphQLList,
  GraphQLNonNull,
} = graphql;

module.exports = new GraphQLSchema({
  //   query: RootQuery,
  //   mutation: Mutation,
});
